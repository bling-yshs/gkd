name: Build-Apk

on:
  workflow_dispatch:

  push:
    branches:
      - '**'
    paths-ignore:
      - 'LICENSE'
      - '*.md'
      - '.github/**'

jobs:
  build:
    if: ${{ !startsWith(github.event.head_commit.message, 'chore:') && !startsWith(github.event.head_commit.message, 'chore(') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '21'

      - uses: gradle/actions/setup-gradle@v5
        with:
          cache-encryption-key: ${{ secrets.GRADLE_CACHE_ENCRYPTION_KEY }}

      - name: write gkd secrets info
        env:
          GKD_STORE_FILE_BASE64: ${{ secrets.GKD_STORE_FILE_BASE64 }}
          GKD_STORE_PASSWORD: ${{ secrets.GKD_STORE_PASSWORD }}
          GKD_KEY_ALIAS: ${{ secrets.GKD_KEY_ALIAS }}
          GKD_KEY_PASSWORD: ${{ secrets.GKD_KEY_PASSWORD }}
        run: |
          if [ -z "$GKD_STORE_FILE_BASE64" ] || [ -z "$GKD_STORE_PASSWORD" ] || [ -z "$GKD_KEY_ALIAS" ] || [ -z "$GKD_KEY_PASSWORD" ]; then
            echo "::warning::GKD signing secrets are incomplete. Falling back to debug signing."
            exit 0
          fi
          printf '%s' "$GKD_STORE_FILE_BASE64" | base64 --decode > "${{ github.workspace }}/gkd.jks"
          if ! keytool -list -keystore "${{ github.workspace }}/gkd.jks" -storepass "$GKD_STORE_PASSWORD" -alias "$GKD_KEY_ALIAS" > /dev/null; then
            echo "::warning::GKD signing secrets are invalid. Falling back to debug signing."
            rm -f "${{ github.workspace }}/gkd.jks"
            exit 0
          fi
          echo GKD_STORE_FILE='${{ github.workspace }}/gkd.jks' >> gradle.properties
          echo GKD_STORE_PASSWORD="$GKD_STORE_PASSWORD" >> gradle.properties
          echo GKD_KEY_ALIAS="$GKD_KEY_ALIAS" >> gradle.properties
          echo GKD_KEY_PASSWORD="$GKD_KEY_PASSWORD" >> gradle.properties

      - run: echo GKD_RENAME_APK_FLAG=1 >> gradle.properties

      - run: chmod 777 ./gradlew
      - run: ./gradlew app:assembleGkdRelease

      - uses: actions/upload-artifact@v7
        with:
          archive: false
          path: app/build/outputs/apk/gkd/release/*.apk

      - uses: actions/upload-artifact@v7
        with:
          name: outputs
          path: app/build/outputs
